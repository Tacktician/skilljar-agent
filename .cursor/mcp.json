{
  "mcpServers": {
    "skilljar-agent": {
      "command": "python3",
      "args": ["${workspaceFolder}/mcp_server.py"],
      "cwd": "${workspaceFolder}",
      "envFile": "${workspaceFolder}/.env",
      "env": {
        "PYTHONPATH": "${workspaceFolder}/src"
      }
    }
  }
}
