Use the SkillJar MCP tools:
1) search_courses for "Developing a Baseline Test Suite (v12)" (or the closest match to that title), max_results 5.
2) Take the best-matching course_id from the results.
3) get_course_content for that course_id.
4) Summarize: course title, lesson count, and for each lesson the title plus the first ~200 characters of content_preview.

If search returns no good match, say so and stop instead of inventing IDs.